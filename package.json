{
  "name": "core-components-monorepo",
  "version": "1.0.0",
  "description": "Enterprise Core Components Monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix calendar && npm install --prefix theme && npm install --prefix autocomplete && npm install --prefix wizard",
    "start:vanilla": "npx vite --config vite.config.ts",
    "build:calendar": "tsc calendar/src/hijri-calendar.lib.ts --outDir calendar/dist --declaration --module ES2020 --target ES2020 --moduleResolution bundler --ignoreDeprecations 6.0",
    "build": "npx vite build",
    "start:portal": "npx -y serve . -p 4000"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.8"
  }
}
