{
  "name": "core-components",
  "version": "1.0.0",
  "description": "High-performance UI components - Zero Dependencies",
  "private": true,
  "scripts": {
    "dev": "npx vite --config vite.config.ts",
    "build": "npx vite build",
    "preview": "npx serve dist -p 3000"
  },
  "devDependencies": {
    "vite": "^8.0.8"
  }
}
